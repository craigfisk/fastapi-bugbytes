**FastAPI (BugBytes) README

Clone the directory.
mkdir fastapi-bugbytes; cd <fastapi-bugbytes>; python3 -m venv .venv-fastapi
source .venv-fastapi/bin/activat
pip install fastapi[standard]
pip install requirements.txt